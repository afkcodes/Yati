import {TextX, ViewX} from '~components/common';

const Profile = () => {
  return (
    <ViewX
      variant="primary"
      flex={1}
      alignItems="center"
      justifyContent="center">
      <TextX>Profile</TextX>
    </ViewX>
  );
};

export default Profile;
